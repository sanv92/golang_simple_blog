package page

/*
package news_test

import (
	"testing"

	"github.com/SanderV1992/golang_simple_blog/news"
)

func testRepo(r news.Repo, t *testing.T) {
	//err := r.create(???)
	if err != nil {
		t.Error(err)
		return
	}

	news, total, err := r.findAll(1, 10)
	if err != nil {
		t.Error(err)
		return
	}

	if total != 1 {
		t.Errorf("too many: %v", total)
	}

	//???
}
*/
