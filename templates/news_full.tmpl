{{ template "header" . }}

<div class="container">
    <div class="row">

        <div class="col-md-12">
            <h1>
                {{ .News.Title }}
            </h1>
            <p>
                {{ .News.Content }}
            </p>
        </div>

    </div>
</div>

{{ template "footer" }}
