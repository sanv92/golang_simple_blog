{{ template "header" . }}

<div class="container">
    <div class="row">

        <div class="col-md-12">
            <h1>
                {{ .News.Title }}
            </h1>
            <a href="/news/edit/?id={{ .News.Alias }}" class="btn btn-primary navbar-btn">Edit</a>
            <p>
                {{ .News.Content }}
            </p>
        </div>

    </div>
</div>

{{ template "footer" }}
