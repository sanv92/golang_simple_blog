{{ template "header" . }}

<div class="container">
    <div class="row">
        <div class="col-md-12">
            ----
            HOME
            ----
            {{.}}
        </div>
    </div>
</div>

{{ template "footer" }}
